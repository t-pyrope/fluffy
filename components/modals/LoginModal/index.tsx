import React from 'react';
import {SModal, SModalContainer} from "../styles";

export const LoginModal = () => {
    return (
        <SModalContainer>
            <SModal>
                LoginModal
            </SModal>
        </SModalContainer>
    )
}
